# elyos-chat

Streaming CLI chat application with LLM tool calling (weather + research APIs).

## Setup

```bash
cp .env.example .env
# Fill in your API keys in .env
pip install -e .
python main.py
```
